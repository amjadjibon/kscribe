---
apiVersion: v1
kind: Service
metadata:
  name: kscribe-dashboard
  namespace: kscribe-system
spec:
  selector:
    app: kscribe-manager
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
