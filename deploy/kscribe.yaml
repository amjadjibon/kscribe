---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: diagnosispolicies.kscribe.amjadjibon.dev
spec:
  group: kscribe.amjadjibon.dev
  names:
    kind: DiagnosisPolicy
    listKind: DiagnosisPolicyList
    plural: diagnosispolicies
    shortNames:
    - dp
    singular: diagnosispolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .spec.llmProvider
      name: Provider
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DiagnosisPolicy is the Schema for the diagnosispolicies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DiagnosisPolicySpec defines the desired behaviour for event-triggered
              diagnosis (REQ-003).
            properties:
              enabled:
                description: Enabled toggles this policy on or off. Defaults to true
                  when omitted.
                type: boolean
              eventReasons:
                description: |-
                  EventReasons is the allowlist of Kubernetes event reasons this policy matches.
                  When empty the operator falls back to its global allowlist.
                items:
                  type: string
                type: array
              llmBaseURL:
                description: LLMBaseURL overrides the operator-level LLM base URL.
                type: string
              llmModel:
                description: LLMModel overrides the operator-level LLM model.
                type: string
              llmProvider:
                description: LLMProvider overrides the operator-level LLM provider
                  for diagnoses matched by this policy.
                type: string
              maxIterations:
                description: MaxIterations caps the diagnosis loop depth for diagnoses
                  under this policy.
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              redact:
                description: Redact controls whether sensitive data is scrubbed from
                  prompts for this policy.
                type: boolean
            type: object
          status:
            description: DiagnosisPolicyStatus holds the observed state of a DiagnosisPolicy.
            properties:
              conditions:
                description: Conditions holds standard Kubernetes condition records.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation this status
                  reflects.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: kscribediagnoses.kscribe.amjadjibon.dev
spec:
  group: kscribe.amjadjibon.dev
  names:
    kind: KscribeDiagnosis
    listKind: KscribeDiagnosisList
    plural: kscribediagnoses
    shortNames:
    - ksd
    singular: kscribediagnosis
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.reason
      name: Reason
      type: string
    - jsonPath: .spec.involvedObjectName
      name: Object
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KscribeDiagnosis is the Schema for the kscribediagnoses API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              KscribeDiagnosisSpec captures the source Kubernetes event and any
              policy/LLM overrides that were active when the diagnosis was triggered.
            properties:
              count:
                description: Count is the number of times the event occurred.
                format: int32
                type: integer
              eventUID:
                description: EventUID is the UID of the source Kubernetes Event.
                type: string
              firstTimestamp:
                description: FirstTimestamp is when the event was first recorded.
                format: date-time
                type: string
              involvedObjectKind:
                description: InvolvedObjectKind is the Kind of the involved object
                  (e.g. Pod, Deployment).
                type: string
              involvedObjectName:
                description: InvolvedObjectName is the name of the Kubernetes object
                  the event referenced.
                type: string
              involvedObjectNamespace:
                description: InvolvedObjectNamespace is the namespace of the involved
                  object.
                type: string
              lastTimestamp:
                description: LastTimestamp is when the event was last seen.
                format: date-time
                type: string
              llmBaseURL:
                description: LLMBaseURL overrides the operator-level LLM base URL
                  for this diagnosis.
                type: string
              llmModel:
                description: LLMModel overrides the operator-level LLM model for this
                  diagnosis.
                type: string
              llmProvider:
                description: LLMProvider overrides the operator-level LLM provider
                  for this diagnosis.
                type: string
              maxIterations:
                description: MaxIterations overrides the maximum diagnosis loop depth.
                format: int32
                maximum: 20
                minimum: 1
                type: integer
              message:
                description: Message is the human-readable event message.
                type: string
              policyRef:
                description: PolicyRef is the name of the DiagnosisPolicy that triggered
                  this diagnosis.
                type: string
              reason:
                description: Reason is the Kubernetes event reason (e.g. BackOff,
                  OOMKilling).
                type: string
              redact:
                description: Redact overrides whether sensitive data is scrubbed from
                  prompts.
                type: boolean
            required:
            - eventUID
            - involvedObjectKind
            - involvedObjectName
            - involvedObjectNamespace
            - message
            - reason
            type: object
          status:
            description: KscribeDiagnosisStatus holds the observed state of a KscribeDiagnosis.
            properties:
              completedAt:
                description: CompletedAt is when the diagnosis loop finished (success
                  or failure).
                format: date-time
                type: string
              conditions:
                description: Conditions holds standard Kubernetes condition records.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              llmModel:
                description: LLMModel records which LLM model was used (SEC-002 audit).
                type: string
              llmProvider:
                description: LLMProvider records which LLM provider was used (SEC-002
                  audit).
                type: string
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation this status
                  reflects.
                format: int64
                type: integer
              persisted:
                description: Persisted indicates whether the RCA result was written
                  to the state DB.
                type: boolean
              phase:
                description: Phase is the current lifecycle phase of this diagnosis.
                enum:
                - Pending
                - Diagnosing
                - Done
                - Partial
                - Failed
                type: string
              promptRedacted:
                description: PromptRedacted indicates whether the prompt was redacted
                  before sending (SEC-002 audit).
                type: boolean
              rootCause:
                description: RootCause is the structured root-cause classification.
                type: string
              startedAt:
                description: StartedAt is when the diagnosis loop began.
                format: date-time
                type: string
              summary:
                description: Summary is the human-readable RCA conclusion.
                type: string
              tokensUsed:
                description: TokensUsed records total tokens consumed by the diagnosis
                  run (SEC-002 audit).
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: v1
kind: Namespace
metadata:
  name: kscribe-system
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kscribe-manager
  namespace: kscribe-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kscribe-role
rules:
- apiGroups:
  - ""
  resources:
  - events
  - nodes
  - pods
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - ""
  resources:
  - pods/log
  verbs:
  - get
- apiGroups:
  - apps
  resources:
  - deployments
  - replicasets
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kscribe.amjadjibon.dev
  resources:
  - diagnosispolicies
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - kscribe.amjadjibon.dev
  resources:
  - kscribediagnoses
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - kscribe.amjadjibon.dev
  resources:
  - kscribediagnoses/status
  verbs:
  - get
  - patch
  - update
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kscribe-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kscribe-role
subjects:
- kind: ServiceAccount
  name: kscribe-manager
  namespace: kscribe-system
---
# PVC for the SQLite state database (CON-006: single replica, single PVC).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kscribe-db
  namespace: kscribe-system
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: kscribe-dashboard
  namespace: kscribe-system
spec:
  selector:
    app: kscribe-manager
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
---
# TASK-036: KSCRIBE_LLM_API_KEY is sourced from the kscribe-llm-secret Secret.
# Create it before deploying: kubectl create secret generic kscribe-llm-secret \
#   --namespace kscribe-system --from-literal=api-key=<your-openai-key>
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kscribe-manager
  namespace: kscribe-system
spec:
  replicas: 1  # CON-006: single replica; Deduper and SQLite are per-process
  selector:
    matchLabels:
      app: kscribe-manager
  template:
    metadata:
      labels:
        app: kscribe-manager
    spec:
      serviceAccountName: kscribe-manager
      containers:
      - name: manager
        image: ghcr.io/amjadjibon/kscribe:latest
        env:
        - name: KSCRIBE_DB_PATH
          value: /data/kscribe.db
        - name: KSCRIBE_ADDR
          value: ":8080"
        - name: KSCRIBE_OPERATOR_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: KSCRIBE_LLM_PROVIDER
          value: openai
        - name: KSCRIBE_LLM_MODEL
          value: gpt-4o-mini
        - name: KSCRIBE_LLM_API_KEY
          valueFrom:
            secretKeyRef:
              name: kscribe-llm-secret
              key: api-key
        - name: KSCRIBE_LEADER_ELECT
          value: "false"
        - name: KSCRIBE_REDACT_ENABLED
          value: "true"
        ports:
        - containerPort: 8080
          name: http
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        volumeMounts:
        - name: db
          mountPath: /data
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
      volumes:
      - name: db
        persistentVolumeClaim:
          claimName: kscribe-db
---
# Default DiagnosisPolicy for the operator namespace.
# Override per-namespace by creating a DiagnosisPolicy in the target namespace.
apiVersion: kscribe.amjadjibon.dev/v1alpha1
kind: DiagnosisPolicy
metadata:
  name: default
  namespace: kscribe-system
spec:
  enabled: true
  eventReasons:
  - BackOff
  - OOMKilling
  - Failed
  - FailedScheduling
  llmProvider: openai
  llmModel: gpt-4o-mini
  maxIterations: 5
  redact: true
